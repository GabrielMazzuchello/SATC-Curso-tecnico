"""Crie um programa que receba uma lista de números inteiros e, usando a função map() com uma
expressão lambda, retorne uma nova lista onde os números ímpares são dobrados. Os números
pares devem permanecer os mesmos."""

listaNumeros = []

for c in range(5):
    num = float(input('Informe um numero: '))
    listaNumeros.append(num)

listaOrdenada = list(map(lambda()))